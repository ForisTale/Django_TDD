DONE Don't save blank items for every request.
DONE Code smell: POST test is too long?
DONE Display multiple items in the table.
DONE Clean up after functional tests.
DONE Remove time.sleeps
DONE Support more than one list!
    DONE Adjust model so that items are associated with different lists.
    DONE Add unique URLs for each list.
    DONE Add a URL for creating a new list via POST.
    DONE Add URLs for adding a new item to an existing list via POST.
DONE Refactor away some duplication in urls.py
TODO Remove hardcoded URLs from views.
TODO Remove hardcoded URL from forms in list.html and home.html